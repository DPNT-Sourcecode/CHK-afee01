# noinspection PyShadowingBuiltins,PyUnusedLocal
def compute(x: float, y: float) -> float:
    """Sum two numbers.

    Args:
        x (float): Number
        y (float): Number

    Returns:
        float: Sum result
    """
    return x + y
